# Base URL for the REST API (no trailing slash).
# Leave empty or set to real API when backend is running.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Set to "true" to use in-app stubbed API (no real backend). Phase 0 only.
NEXT_PUBLIC_USE_STUB_API=true
